#pragma once
#include<vector>
using namespace std;
void dectobin(unsigned char num, vector<unsigned char> &v);
unsigned char* parseArray(unsigned char* src,long srcLen);
unsigned char* inverseParse(unsigned char* src, long srcLen);